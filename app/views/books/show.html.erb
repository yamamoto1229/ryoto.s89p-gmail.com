<p class="single-text">
	<% if flash[:notice] %>
      <div class="flash">
        <%= flash[:notice] %>
      </div>
    <% end %>
    <%= @book.title %>
    <%= @book.body %>
</p>
<!-- 編集部分 -->
<%= link_to "Edit", edit_book_path(@book) %>

<%= link_to "Back", books_path %>